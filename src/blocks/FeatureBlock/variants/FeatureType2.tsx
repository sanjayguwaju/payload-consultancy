import RichText from '@/components/RichText'
import Image from 'next/image'
import React from 'react'

export const FeatureType2 = (props: any) => {
  const { id, introContent, media } = props
  return (
    <>
      <div>
        <h1>This is feature block 2</h1>
      </div>
    </>
  )
}